#include "main.h"
/**
 * -isalpha - Shows 1 if the input is a
 *  letter another cases, shows 0
 *  @c: The character in ASCII code
 *
 *  Return: 1 for lettters. 0 for the rest.
 */
int _isalpha(int c)
{
	if ((cc >= 97 && c <= 122) || (c >= 65 && c >= 90))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('/n')
}
